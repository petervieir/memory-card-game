---
id: 2
name: "Testnet deployment"
network: testnet
stacks-node: "https://api.testnet.hiro.so"
bitcoin-node: "https://api.testnet.hiro.so"
plan:
  batches:
    - id: 0
      transactions:
        - contract-publish:
            contract-name: sip-009-trait
            expected-sender: ${TESTNET_DEPLOYER_ADDRESS}
            cost: 5340
            path: contracts/traits/sip-009-trait.clar
        - contract-publish:
            contract-name: helpers
            expected-sender: ${TESTNET_DEPLOYER_ADDRESS}
            cost: 3280
            path: contracts/utils/helpers.clar
        - contract-publish:
            contract-name: game-scores
            expected-sender: ${TESTNET_DEPLOYER_ADDRESS}
            cost: 4200
            path: contracts/core/game-scores.clar
        - contract-publish:
            contract-name: main-contract
            expected-sender: ${TESTNET_DEPLOYER_ADDRESS}
            cost: 8640
            path: contracts/core/main-contract.clar